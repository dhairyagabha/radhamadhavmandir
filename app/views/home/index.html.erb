<%= render 'layouts/carousel' %>
<% @article = @articles.tagged_with('home').first %>
<div class="container">
  <h3 class="text-center"><%= @article.title.titleize %></h3>
  <div class="row align-items-center">
    <%= image_tag 'https://placeimg.com/567/447/nature', class: 'col-sm-12 col-md-6 responsive-img' %>
    <div class="col-sm-12 col-md-6">
      <%= raw @article.body %></p>
    </div>
  </div>
</div>